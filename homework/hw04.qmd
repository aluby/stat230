---
title: "Homework 04"
format: html
engine: knitr
editor: source
navbar: false
execute: 
  message: false
  warning: false
---

```{r}
#| echo: false
#| warning: false
#| message: false

library(ggplot2)
library(ggformula)
library(here)
library(broom)
library(tidyverse)
```

Due: Friday of Week 4 at start of class

There is a [homework template](../files/hw-template.qmd) available if you'd like to use that as a starter file, and copy and paste the code as needed. 

::: callout-important
## Important! 

I will add problems after each class day. Be sure to check back here after each class meeting for new problems.
:::


# Problem 1: Log transformed response

<!-- Math-y transformation problem (interpreting a log transform?)  -->

In class, we talked about how we must be careful about interpreting estimated $\beta$ coefficient when we've transformed the variables since the relationship is typically no longer constant for the entire range of the predictor variable.  (e.g. the [wildfires](https://aluby.github.io/stat230/slides/slides08.html#/interpreting-the-model) example, which *decreases* from 1960-1980-ish and then increases at a non-constant rate) One exception is log transformations, which we can usually "math-out" a reasonable interpretation on the original variable scale. 

A data science team at a tech startup is analyzing the relationship between their weekly digital advertising budget and their weekly app downloads. Because both advertising budgets and user growth often scale multiplicatively rather than additively, the team decides to test models using the natural logarithm.

- Let $Y$ = `Downloads` (in thousands of users)
- Let $X$ = `Ad_Spend` (in thousands of dollars)

(a) Show that, if we use the fitted regression equation $\widehat{ \log y_i} = \widehat{\beta_0} + \widehat{\beta_1} x$, then $\hat{y_i} = e^{\widehat{\beta_0}} e^{\widehat{\beta_1} x_i}$

(b) Use part (a) to show or explain why this means that we can interpret $\widehat{\beta_1}$ as follows: "When $x$ increases by 1 unit, we expect $y$ to **multiply** by $e^\widehat{\beta_1}$"

(c) If we find the following fitted equation $\widehat{\log y} = 2.4 + 0.08 x$, provide an interpretation of $\widehat{\beta_1}$ in terms of percentage change in raw downloads

# Problem 2: Multiple regression (no R needed)

Peterson and Ziegler (2021) scraped data on LEGO® sets from themes produced January 1, 2018 and September 11, 2020 from Brickset.com. In this problem, you will work with data on about 400 randomly selected LEGO sets produced during this time period. The data contains the following variables:

- `pieces`: Number of pieces in the set from brickset.com.
- `minifigures`: Number of minifigures (LEGO® people) in the set scraped from brickset.com. - `amazon_price`: Price of the set on Amazon.com (in U.S. dollars)

Below is output from the multiple regression model fit in R: 

```{r}
#| echo: false

lego <- read.csv("https://aloy.github.io/stat230-materials/data/lego1.csv")

lego_lm <- lm(formula = amazon_price ~ pieces + minifigures, data = lego)

summary(lego_lm)
```

::: callout-note
## Note

You do not need to do any R for this question, just use the output to answer the questions
:::

a. Report the fitted regression equation.
b. Interpret each slope coefficient in context.
c. Explain why the intercept is not meaningful in this context.
d. What proportion of the variability in the Amazon price of a LEGO set is explained by the number of pieces and the number of minifigures?
e. Calculate a 90% confidence interval for the slope associated with pieces.
f. Interpret your confidence interval from part (e) in context.


# Problem 3: Mon

The {palmerpenguins} data were collected and made available by Dr. Kristen Gorman and the Palmer Station, Antarctica LTER, a member of the Long Term Ecological Research Network. This data contains measurements of penguins' body mass, flipper length, and bill size; along with their sex and species. There are 3 penguin species included: Gentoo, Chinstrap, and Adelie. We are interested in building a model to predict flipper length using body mass for each of the 3 species. 

![](https://allisonhorst.github.io/palmerpenguins/reference/figures/lter_penguins.png)
Use the summary R output below to answer the questions (no R code needed)

```{r}
#| echo: false

penguins_mlr <- lm(flipper_length_mm ~ body_mass_g + species + body_mass_g:species, data = palmerpenguins::penguins)

summary(penguins_mlr)
```

(a) Write out the fitted regression equation for Chinstrap penguins

(b) Write out the fitted regression equation for Gentoo penguins

(c) Write out the fitted regression equation for Adelie penguins

(d) Give a careful interpretation for the $\beta$ coefficient labeled `body_mass_g:speciesChinstrap` in the context of the problem


# Problem 4: Mon

<!--  categorical predictor (in R)  -- Ames housing? -->

The `ames_houses` dataset contains data on sale prices of 232 houses in Ames, Iowa. It was compiled by the Ames Assessor’s Office and used for computing assessed values for individual residential properties sold in Ames, IA from 2006 to 2010. 

The original dataset included data on nearly 3000 houses and included 82 variables. For this problem, we’re going to focus on explaining `Sale_Price` using `Square_Feet`, `Lot_Area`, and `Fireplace`. 


```{r}
ames_houses = read_csv("https://aluby.github.io/stat230/data/ames_houses.csv")
```

(a) Make a scatterplot of `Sale_Price` against `Square_Feet` and report the fitted regression equation. Summarize your main result in 1 sentence

(b) Check the SLR conditions and report your findings

(c) There are additional predictors in this dataset. Make a pairs plot of `Sale_Price`, `Square_Feet`, `Lot_Area`, and `Fireplace`. Summarize the main results in 1 sentence.

(d) Fit a parallel lines MLR model that uses all 3 predictors and report the fitted regression equation. 

(e) How did $\beta_{\text{SquareFeet}}$ change from part (a)? 

(f) Make a single EDA plot that shows the relationship between `Square_Feet`, `Fireplace`, and `Sale_Price`. Does your plot suggest a different slopes model is needed?

# Problem 5: Wed

<!-- inference for MLR  -->

# Problem 6: Wed

<!-- inference for MLR: return to Ames housing data  -->
